Application Test run module -> jmp-cloud-bank-service-app , run BankServiceApp.main to execute
ServiceLoader Test run module -> jmp-cloud-bank-service-client , run BankServiceClient.main to execute


Collectors.toUnmodifiableList() can be replaced by .toList() since it performs the same task of creating a unmodifiable list hence the implementations have used the same.


